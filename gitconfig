[include]
        path = .gitconfig.secret
[push]
	default = current
[pager]
	branch =
	diff = diff-so-fancy | less --tabs=1,5 -RFX
	show = diff-so-fancy | less --tabs=1,5 -RFX
[mergetool]
	keepBackup = false
[merge]
	tool = meld
[diff]
	tool = vimdiff
[difftool]
	prompt = false
[commit]
	verbose = true
